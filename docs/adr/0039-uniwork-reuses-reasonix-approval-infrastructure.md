# Uniwork reuses Reasonix approval infrastructure

Uniwork operations will reuse Reasonix permission and approval infrastructure rather than creating a separate approval system. The host maps typed Uniwork Intents into approval requests and execution, but user-facing prompts should use Uniwork semantics and risk labels instead of exposing raw shell commands as the primary action.

**Status**: accepted
