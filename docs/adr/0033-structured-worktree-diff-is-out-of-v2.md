# Structured Worktree diff is out of V2

Uniwork V2 will not require a structured Worktree diff model because Univer tooling does not currently expose that capability. Worktree UX should still cover status, open, merge, and discard flows, and a future structured diff can be added through capabilities and typed blocks when the underlying tooling exists.

**Status**: accepted
