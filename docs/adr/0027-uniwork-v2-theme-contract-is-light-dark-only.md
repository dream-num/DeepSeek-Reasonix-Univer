# Uniwork V2 theme contract is light and dark only

V2 will pass only a light or dark Uniwork Color Scheme from the host into `@univer/uniwork`. Reasonix keeps its full desktop theme system inside the Uniwork Shell, while the Uniwork Surface adapts to the basic color scheme without introducing a broader theme-token contract until a concrete need appears.

**Status**: accepted
