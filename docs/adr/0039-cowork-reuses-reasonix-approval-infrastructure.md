# Cowork reuses Reasonix approval infrastructure

Cowork operations will reuse Reasonix permission and approval infrastructure rather than creating a separate approval system. The host maps typed Cowork Intents into approval requests and execution, but user-facing prompts should use Cowork semantics and risk labels instead of exposing raw shell commands as the primary action.

**Status**: accepted
