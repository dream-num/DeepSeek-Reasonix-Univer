# Cowork package is published by Univer tooling

`@univer/cowork` belongs to the Univer tooling repository and release pipeline, not to Reasonix. The package owns Univer frontend embedding and contract exports, while Reasonix consumes a pinned package version and implements its host adapter against the published Cowork contracts.

**Status**: accepted
